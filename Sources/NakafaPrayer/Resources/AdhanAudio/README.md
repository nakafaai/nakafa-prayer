# Adhan Audio

Place a release-approved `adhan.mp3` in the app bundle resources before shipping
if you want a real recorded adhan. The app already looks for that file and then
falls back to the localized spoken reminder.

Do not commit audio unless Nakafa has the rights to redistribute it.
